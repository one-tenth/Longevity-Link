[phases.setup]
nixPkgs = ["python311", "pip"]

[phases.build]
cmds = ["cd back_end && pip install -r requirements.txt"]

[phases.start]
cmd = "cd back_end && python manage.py migrate && python manage.py runserver 0.0.0.0:8000"

